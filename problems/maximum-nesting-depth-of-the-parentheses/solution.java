            count--;
        }
        if (s.charAt(i) == ')') {
        maxCount = Math.max(count, maxCount);
        }
        if (s.charAt(i) == '(') {
            count++;
    for (int i = 0; i < s.length(); i++) {
    int count = 0;
    }
    return maxCount;
    }
    public static int maxDepth(String s) {
        // re-solve 
        return maxDepthParantheses(s);
    }