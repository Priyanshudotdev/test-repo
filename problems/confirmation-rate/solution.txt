    ) confirmation_rate
        ), 2
            END
                ELSE 0
                WHEN c.action = 'confirmed' THEN 1.00
            CASE 
        AVG(
    ROUND(
SELECT si.user_id,
FROM Signups si
LEFT JOIN Confirmations c
ON
si.user_id = c.user_id
GROUP BY si.user_id;