                left++;
            }
            if (sum == rem) longest = Math.max(longest, right - left + 1);
        }
    }
    public int notWorkingMethod(int[] nums, int x){
        int left = 0;
       int right = nums.length - 1;
       int op = 0;
                sum -= nums[left];
            while (sum > rem) {
            sum += nums[right];
        for (int right = 0; right < n; right++) {
        if(rem == 0) return n;
        if(rem < 0) return -1;
        return longest == 0 ? -1 : n - longest;
       int currSum = x;