    }
    static int bSearch2(int[] nums, int target, boolean flag){
        int low = 0;
        int high = nums.length - 1;
        int result = -1;
        return new int[]{index1, index2};
        int index2 = bSearch2(nums, target, false);
        int index1 = bSearch2(nums, target, true);
    static int[] reSove2(int[] nums, int target){
        index or 2nd"
     */
        - but how do we know when we have to do first index or second ?
        - wil init a flag boolean var which will tell the b search that "search for 1st 
        - one for first index and second for second index
        - will do two b search
        - Optimal Solution
    /**