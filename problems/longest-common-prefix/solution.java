    }
        }
            }
                return ref.substring(0, i);
            if (i >= str.length() || str.charAt(i) != ref.charAt(i)) {
        for (String str : strs) {
    for (int i = 0; i < ref.length(); i++) {
    String ref = strs[0];
    public static String reSolve(String[] strs) {
    return ref;
    // re-solve
        return reSolve(strs);
    }
    public String longestCommonPrefix(String[] strs) {
class Solution {
    }