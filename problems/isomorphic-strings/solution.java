    public static boolean optimalMethod(String s, String t){
    }
        return true;
        }
            m2[t.charAt(i)] = i + 1;
            m1[s.charAt(i)] = i + 1;
            }
                return false;
            if (m1[s.charAt(i)] != m2[t.charAt(i)]) {
        int[] m1 = new int[256], m2 = new int[256];
        for(int i = 0; i < s.length(); i++){
            if(m1[s.charAt(i)] != m2[t.charAt(i)]) return false;
            m1[s.charAt(i)] = i + 1;
        for (int i = 0; i < s.length(); i++) {
        int[] m2 = new int[256];