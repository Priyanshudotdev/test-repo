        if (top > bottom) return false;
            }
        }
            } else if (target < arr[mid][0]) {
                bottom = mid - 1;
            } else {
                break;
            if (target > arr[mid][cols - 1]) {
                top = mid + 1;
        while (top <= bottom) {
            mid = top + (bottom - top);
        int top = 0;
        int bottom = rows - 1;
        int mid = 0;
        int cols = arr[0].length;
    public boolean optimalMethod(int[][] arr, int target){
        int rows = arr.length;
    }
        
        return optimalMethod(matrix, target);
    public boolean searchMatrix(int[][] matrix, int target) {
class Solution {