        }
            return;
        if(breakPoint == 0){
            reverseArr(nums, 0, nums.length - 1);
        // System.out.println("elemIndex " + breakPoint);
            }
        }  
        
            if(nums[i - 1] < nums[i]){
                breakPoint = i;
                break;
        for(int i = nums.length - 1; i > 0; i--){
        //step-1 : find the break point where (from back) where a[i] < a[i+1]
        
        int breakPoint = 0;
        int elemIndex = 0;
    // re-re-solve
class Solution {
    static void nextPermutation(int[] nums){