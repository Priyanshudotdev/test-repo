     */
        will remove all elements til we get out first "a" 
    public static int optimalMethod(String s){
        Set<Character> set = new HashSet<>();
        - why ? : let say : s = "abcabcbb", here we reach till c and our substring looks like "abc" so 
        substring
        *Optmizid Algorithm*
        - will check each string and if we got any duplicate string will from the element from that 
    public static int lengthOfLongestSubstring(String s) {
        return optimalMethod(s);
    }
    /**
class Solution {
import java.util.HashSet;
import java.util.Set;