        List<Integer> list = new ArrayList<>();
        if (num == 0) {
            list.add(0);
            return list;
        }
        while (num > 0) {
            int digit = num % 10;
            list.add(digit);
            num /= 10;
        }
        return list;
    }
}
    public List<Integer> getDigitsOfNum(int num) {
    }
        return reversed;
        }