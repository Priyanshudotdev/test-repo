            for(int j = 0; j < i; j++){
                lmul *= nums[j];
            }
            // for right
            for(int j = i + 1; j < n; j++){
                rmul *= nums[j];
            }
            // for left
            int rmul = 1;
        for(int i = 0; i < n; i++){
            int lmul = 1;
        int[] result = new int[nums.length];
        int n = nums.length;
    public int[] optimalMethod(int[] nums){
        - at last just multiple left*right and store it in result array
     */
        - same for right
        - left array where all elements left answers will be store
        - create two arrays to store multiplication result 
        - Optimal Method
    /**