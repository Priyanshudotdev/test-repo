            leftSum = 0;
            r--;
        }else {
            rightSum = 0;
            l++;
        }
        // l += 1;
        // r -= 1;
        while(l < r){
        }
        
        // if(l == r && leftSum != rightSum) return -1;
        
        if(leftSum >= rightSum) {
        int rightSum = nums[r];
        int leftSum = nums[l];
        int r = nums.length - 1;
        int l = 0;
    public int bruteForceMethod(int[] nums){
    }
        return bruteForceMethod(nums);