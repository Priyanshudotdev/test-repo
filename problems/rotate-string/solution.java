    }
    public static boolean rotateString(String s, String goal) {
        return resolve(s,goal);
    }
        return false;
        }
            return true;
        if ((s + s).contains(goal)) {
        false;
    public static boolean resolve(String s, String goal){
        if(s == null || goal == null || s.length() != goal.length() return 
    // re-solve
class Solution {