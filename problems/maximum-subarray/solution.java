            - Resolve
            - take two vars : one to store the sum and one to store max sum (result)
            - check if currSum is less than zero or not
        /*
    public int optimalSol(int[] nums){
    }
        
        return sum;
                tempSum = 0;
            }
        }
            tempSum += nums[i];
            sum = Math.max(tempSum, sum);
            if(tempSum < 0){
        int sum = nums[0];
        int tempSum = 0;
        for(int i = 0; i < nums.length; i++){
    public int reSolve(int[] nums){
    }
        return reSolve(nums);
    public int maxSubArray(int[] nums) {
class Solution {