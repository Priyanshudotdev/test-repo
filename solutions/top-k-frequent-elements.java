List<Integer>[] buckets = new List[nums.length + 1];for (var e : freq.entrySet()) {int num = e.getKey();int f = e.getValue();if (buckets[f] == null) buckets[f] = new ArrayList<>();buckets[f].add(num);}int[] result = new int[k];int idx = 0;for (int i = buckets.length - 1; i >= 0 && idx < k; i--) {if (buckets[i] == null) continue;for (int num : buckets[i]) {result[idx++] = num;if (idx == k) break;}for (int n : nums) freq.put(n, freq.getOrDefault(n, 0) + 1);}return result;}}