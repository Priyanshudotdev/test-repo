/*- one loop from start and one from end ??- if arr[mid] > arr[mid + 1]-      2      >    1*/while(low < high){int mid = low + (high - low) / 2; // 1if(arrget(mid) > arr.get(mid + 1)) {high = mid; // 0}else{low = mid + 1;}// else if(arr[mid] < arr[mid + 1])}// we can print low or end because both are pointing at same indexreturn low;}